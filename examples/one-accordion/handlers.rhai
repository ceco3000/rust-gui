// WaAccordion handlers.rhai
// Accordion items are self-contained — clicking headers toggles expand/collapse
// without needing explicit Rhai handlers.
// The widget_instance handler in the framework manages the state.
