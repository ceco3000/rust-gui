# 审核判决书：P05a / F02b 任务设计

> 主审：review-court 四角色辩论
> 被审文档：D8-阶段0开发任务分解.md（新增行 P05a、F02b）、D12-进度跟踪.md（新增行）
> 审核日期：2026-06-21

## 1. 文档标识和类型分类

- **主类型**：`设计`（任务分解文档） + `计划`（进度跟踪）
- **审核目标**：D8 新增 P05a/F02b 任务 + D12 新增跟踪条目的设计质量、依赖一致性、可执行性
- **核心主张**：将当前基于 HitTester 的平铺命中测试升级为 DFS 树遍历方案，分两步（F02b 提供数据 → P05a 实现遍历）

## 2. 辩护方观点

1. **任务拆分合理，依赖清晰**：P05a 依赖 P05、F02b；F02b 前置 P05a。均为 P1，不阻塞核心交付。D12 状态标记准确。
2. **验收标准具体可测**：P05a 验收可直接用 accordion demo 验证；F02b 验收可通过编译检查 + 单元测试。
3. **与主流框架对齐**：明确引用 Flutter/浏览器方案，技术方案描述充分（DFS 遍历、父子坐标转换、最深命中 WidgetId）。

## 3. 起诉方观点

1. **F02b 的泛型问题未解决** ⚠️：验收标准写"无需 M 泛型"，但 `WidgetView<M>` 的 M 在当前 App 中不可消解。这是未解决的技术阻塞点。
2. **前置 D5 设计文档未更新** 🔴：命中测试从平铺改为树遍历属于 D5 范畴，D8 任务未声明需同步更新 D5。
3. **与现有 init_widget_instances 存在架构冲突** 🟠：当前代码注册 rect 到 HitTester，树遍历方案下此步将成为冗余。D8 未说明迁移策略。

## 4. 证据核查

| 证据 | 来源 | 核实 |
|------|------|:---:|
| D8 P05a 依赖 P05、F02b | D8 新增行 | ✅ |
| D8 F02b 依赖 F02 | D8 新增行 | ✅ |
| App 当前无 M 泛型 | rgui/src/app.rs struct App | ✅ |
| WidgetView 需要 M 泛型 | rgui-core/src/view.rs | ✅ |
| D5 §4 描述 HitTester 平铺模式 | rgui-platform/src/hit_test.rs 原实现 | ✅ |
| init_widget_instances 注册 rect | rgui/src/interactive.rs:169 | ✅ |

## 5. 按标准逐项审核

| 标准 | 结果 | 说明 |
|------|:---:|------|
| U1 目标与范围清晰性 | ✅ | 目标明确：树遍历命中测试 |
| U2 上下文与需求对齐 | ✅ | 对齐主流框架 |
| U3 事实与依据可靠性 | ⚠️ | F02b 泛型方案模糊 |
| U4 逻辑一致性 | ⚠️ | 两任务互依赖顺序不明 |
| U5 完整性与遗漏 | 🔴 | 缺 D5 更新、缺迁移说明 |
| U6 风险与边界条件 | ⚠️ | 未提 HitTester 兼容风险 |
| U7 可验证性与可执行性 | ✅ | 验收标准可测 |
| U8 表达清晰度 | ✅ | 中文描述清晰 |

| 专属标准（设计） | 结果 |
|------|:---:|
| 方案适配性 | ⚠️ |
| 权衡与替代方案 | ⚠️ |
| 故障模式 | ⚠️ |

## 6. 严重度

| # | 严重度 | 问题 |
|---|:---:|------|
| 1 | 🟠 严重 | 缺少 D5 前置更新依赖 |
| 2 | 🟠 严重 | F02b 泛型解决方案不明 |
| 3 | 🟡 中等 | 旧 HitTester 迁移策略缺失 |

## 7. 最终判决

**🔧 有条件通过**

## 8. 必须修复项

1. **D8 P05a/F02b 依赖列中加入 '更新 D5 §4 命中测试设计'**
2. **F02b 验收标准中明确泛型解决方案**（type erasure with `Box<dyn Any>` 或关联类型）
3. **D8 补充旧 HitTester 的迁移策略**（保留作为 onclick fallback vs 完全替换为树遍历）

## 9. 后续行动

- 修复上述 3 项后重新投票
- 实现时 TDD 先行（先写命中测试用例，再实现 DFS 遍历）
- 建议先完成 F02b（提供 WidgetView 访问），再实现 P05a（树遍历命中测试）
