# D7：开发反馈系统设计

> **文档定位：** 本文档是《Rust GUI 框架总体设计》（D0）的子系统详细设计，定义三层反馈实现、双进程架构、状态恢复和降级策略。

> **前置阅读：** [Rust GUI 框架总体设计](./Rust%20GUI%20框架总体设计.md)（D0）§4.8（rgui-devtools API）、[D2 状态管理与差分更新设计](./D2-状态管理与差分更新设计.md)（快照与迁移协议）。

> **对应路线书章节：** §5.4。

> **状态：** 初版。

---

## 目录

1. [设计目标与范围](#1-设计目标与范围)
2. [三层反馈架构](#2-三层反馈架构)
3. [第 1 层：样式热重载](#3-第-1-层样式热重载)
4. [第 2 层：结构热重载](#4-第-2-层结构热重载)
5. [第 3 层：Rust 逻辑反馈（快速重启）](#5-第-3-层-rust-逻辑反馈快速重启)
6. [双进程架构](#6-双进程架构)
7. [IPC 协议设计](#7-ipc-协议设计)
8. [状态恢复与序列化](#8-状态恢复与序列化)
9. [降级策略](#9-降级策略)
10. [脚本热重载（预留）](#10-脚本热重载预留)
11. [边界情况处理](#11-边界情况处理)
12. [验证标准](#12-验证标准)

---

## 1. 设计目标与范围

### 1.1 本文档解决什么问题

1. 定义三层反馈系统的完整架构和每层的实现方案
2. 定义 DisplayProcess + AppProcess 双进程架构及职责边界
3. 定义 IPC 协议（快照传输、事件传递、场景图同步）
4. 定义快速重启时的状态序列化、传输和恢复流程
5. 定义各级降级策略（重启失败不丢失状态）

### 1.2 关键数值目标

| 反馈层 | 目标延迟 | 适用文件 |
|--------|---------|---------|
| 第 1 层 | < 200ms | `.rgss` 样式文件 |
| 第 2 层 | < 1s | `.rgui` 结构文件 |
| 第 3 层 | 2-5s | `.rs` Rust 源文件 |
| 第 4 层（预留） | < 500ms | `.rhai` 脚本文件 |

---

## 2. 三层反馈架构

```
反馈层级与时序：

第 1 层（样式）     ████                             < 200ms
第 2 层（结构）     ████████████                     < 1s
第 4 层（脚本）     ██████                           < 500ms（预留）
第 3 层（Rust）     ████████████████████████████████  2-5s
```

### 2.1 文件监控

使用 `notify` crate 监控项目目录。监控策略：

- 递归监控 `src/`、`styles/`、`assets/`
- Debounce：合并 300ms 内的多次保存为一次重载
- 按文件扩展名路由到对应的热重载处理器

```rust
pub struct DevTools {
    watcher: notify::RecommendedWatcher,
    style_reloader: StyleHotReload,
    debounce_timer: Option<Instant>,
}

impl DevTools {
    pub fn new(project_dir: &Path) -> Result<Self, notify::Error> {
        let mut watcher = notify::recommended_watcher(|event| {
            // 事件处理见子模块
        })?;
        watcher.watch(project_dir, notify::RecursiveMode::Recursive)?;
        Ok(DevTools { watcher, style_reloader: StyleHotReload::new(project_dir.join("styles")).map_err(|e| notify::Error::new(notify::ErrorKind::Generic(e.to_string())))?, debounce_timer: None })
    }
}
```

---

## 3. 第 1 层：样式热重载

详见 [D4 §7](./D4-样式系统与rgss设计.md#7-样式热重载)。核心流程：`notify` → 重新解析 `.rgss` → 比较新旧样式表 → 标记受影响 widget dirty → 下一帧自动应用。

---

## 4. 第 2 层：结构热重载

`.rgui` 文件以类 XML 语法描述 UI 结构。阶段 2 完善。

### 4.1 流程

```
notify 检测 .rgui 变更 → 重新解析 → WidgetView diff → Patch → layout + paint
```

### 4.2 App 集成 API（阶段 2 实现）

`rgui` facade crate 通过 `AppConfig` 和 `App` 方法暴露 `.rgui` 加载能力。

#### AppConfig 新增字段

```rust
pub struct AppConfig {
    // ... 现有字段 ...
    /// 可选 `.rgui` 文件路径，作为视图源（替代 set_view_scene_builder）。
    /// 设置后 App 启动时自动解析 `.rgui` → `WidgetView`，并启用热重载。
    pub rgui_path: Option<PathBuf>,
}
```

#### App 新增方法

```rust
impl App {
    /// 从 `.rgui` 文件加载视图（设置 view_scene_builder）。
    ///
    /// `M` 为 AppMessage 类型，用于解析器泛型参数。
    /// 内部创建 RguiHotReload<M>，每帧轮询文件变更，
    /// 变更时重新解析 → compute_view_layout → build_scene_from_view → SceneGraph。
    ///
    /// 解析失败时保持旧视图，通过 stderr 报告错误（D7 §9 降级策略）。
    pub fn load_rgui<M: AppMessage>(&mut self, path: impl AsRef<Path>) -> Result<(), RguiHotReloadError>;
}
```

#### AppHandler 集成点

1. **启动时**（`resumed()`）：
   - 若 `config.rgui_path` 非空，调用 `app.load_rgui::<M>(path)`
   - `M` 由用户在调用 `run()` 前通过 turbofish 指定

2. **每帧渲染前**（`RedrawRequested`）：
   - `RguiHotReload.check_and_reload()` → 若有新视图 → 自动更新 SceneGraph
   - 更新逻辑已封装在 `load_rgui` 创建的 `ViewSceneBuilder` 闭包中

3. **降级策略**（D7 §9）：
   - `.rgui` 解析失败 → 保持旧视图，stderr 输出错误
   - 文件不存在 → `load_rgui` 返回 `Err`

#### 依赖关系

```
rgui (facade) ──→ rgui-devtools (feature: devtools, 现有依赖)
rgui-devtools ──→ rgui-script (RH03 已添加依赖)
rgui (facade) ──→ rgui-script (新增，feature: devtools)
```

#### 用户使用示例

```rust
use rgui::app::{App, AppConfig};
use rgui::AppMessage;

#[derive(Debug, Clone, PartialEq, AppMessage)]
enum Msg { Clicked }

fn main() -> Result<(), Box<dyn std::error::Error>> {
    let config = AppConfig::new()
        .title("rgui App")
        .rgui_path("ui/app.rgui");  // [V4] 新增

    let mut app = App::new(config);
    app.register_defaults();
    app.load_rgui::<Msg>("ui/app.rgui")?;  // [V4] 新增
    app.load_rhai_scripts(&["scripts/handlers.rhai"])?;  // [V4] 新增
    app.run()
}
```

---

## 5. 第 3 层：Rust 逻辑反馈（快速重启）

### 5.1 完整流程

```
1. .rs 文件保存
2. cargo build 增量编译（sccache，目标 2-5s）
3. DisplayProcess 检测二进制更新
4. 捕获当前状态快照 + RestoreMetadata
5. DisplayProcess 启动新的 AppProcess
6. IPC 发送 IpcMessage::RestoreState { snapshot, metadata }
7. 新 AppProcess 恢复 StateStore
8. 所有 widget 重新 view()
9. 恢复焦点、滚动位置
10. DisplayProcess 渲染第一帧
11. 旧 AppProcess 退出
```

### 5.2 序列图

```
DisplayProcess              Cargo              AppProcess(new)
     │                        │                      │
     │ .rs 变更                │                      │
     │──→ cargo build ───────→│                      │
     │                        │← 编译完成 ───────────│
     │←── 通知 ──────────────│                      │
     │                        │                      │
     │ 捕获快照 + 元数据       │                      │
     │──→ 启动新进程 ─────────┼─────────────────────→│
     │──→ IPC: RestoreState ─┼─────────────────────→│
     │                        │                      │ 恢复 StateStore
     │                        │                      │ view() 重建
     │←── IPC: Ready ────────┼──────────────────────│
     │ 替换渲染源              │                      │
     │──→ IPC: Shutdown ─────┼─────────────────────→│ (旧进程)
```

---

## 6. 双进程架构

### 6.1 进程职责

```
┌──────────────────────┐     IPC (Unix Domain Socket / Named Pipe)
│   DisplayProcess      │◄══════════════════════════════════►
│                       │                                    │
│  - 窗口管理 (winit)    │         ┌──────────────────────────┴──────────┐
│  - GPU 渲染 (wgpu)    │         │     AppProcess                       │
│  - 输入事件接收        │         │                                      │
│  - 文件监控 (notify)   │         │  - Widget 树维护                     │
│  - 热重载触发          │         │  - StateStore 管理                   │
│                       │         │  - 业务逻辑执行                      │
└──────────────────────┘         │  - view()/update()/paint() 调用      │
                                  └─────────────────────────────────────┘
```

### 6.2 设计理由

- **进程隔离**：AppProcess panic 不影响 DisplayProcess，窗口保持显示
- **GPU 上下文保持**：DisplayProcess 重启，AppProcess 独立重启
- **编译隔离**：AppProcess 二进制独立编译，DisplayProcess 不参与重编译

### 6.3 阶段 1 简化

阶段 1（MVP）中，DisplayProcess 和 AppProcess 合并为单进程。双进程架构在阶段 2 引入。阶段 1 的"快速重启"降级为普通重启（退出 → `cargo build` → 手动重新启动），状态通过文件系统保存和恢复。

---

## 7. IPC 协议设计

```rust
/// IPC 消息协议（serde 序列化，postcard 二进制格式）。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub enum IpcMessage {
    /// DisplayProcess → AppProcess：状态快照 + 元数据
    RestoreState { snapshot: Vec<u8>, metadata: RestoreMetadata },

    /// AppProcess → DisplayProcess：就绪信号
    Ready { widget_count: u32 },

    /// AppProcess → DisplayProcess：场景图更新（全量或增量）
    SceneUpdate { scene_data: Vec<u8>, incremental: bool },

    /// DisplayProcess → AppProcess：输入事件
    InputEvent { event_data: Vec<u8> },

    /// DisplayProcess → AppProcess：终止信号
    Shutdown,

    /// AppProcess → DisplayProcess：可恢复错误
    Error { message: String, recoverable: bool },
}

/// IPC 传输层。
pub struct IpcChannel {
    stream: UnixStream,  // Unix: UnixStream; Windows: NamedPipe
    read_buffer: Vec<u8>,
}

impl IpcChannel {
    pub fn send(&mut self, msg: &IpcMessage) -> Result<(), IpcError> {
        let data = postcard::to_vec(msg)?;
        // 写入长度前缀 + 数据
        let len = data.len() as u32;
        self.stream.write_all(&len.to_le_bytes())?;
        self.stream.write_all(&data)?;
        Ok(())
    }

    pub fn recv(&mut self) -> Result<IpcMessage, IpcError> {
        let mut len_buf = [0u8; 4];
        self.stream.read_exact(&mut len_buf)?;
        let len = u32::from_le_bytes(len_buf) as usize;
        self.read_buffer.resize(len, 0);
        self.stream.read_exact(&mut self.read_buffer)?;
        Ok(postcard::from_bytes(&self.read_buffer)?)
    }
}
```

---

## 8. 状态恢复与序列化

### 8.1 传输格式

```rust
/// RestoreMetadata：快照前的交互上下文（D2 §8.2 已有定义，此处引用）。
/// 使用 HashMap 以兼容 serde（FxHashMap 不实现 Serialize/Deserialize）。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RestoreMetadata {
    pub focus_path: Vec<u64>,
    pub scroll_positions: HashMap<u64, (f64, f64)>,
    pub current_route: String,
    pub window_geometry: (i32, i32, u32, u32),
}
```

### 8.2 恢复流程

```
AppProcess 接收 IpcMessage::RestoreState { snapshot, metadata }
  → Snapshotter::restore(snapshot, &mut state_store, &migrations)  // 需在 D2 Snapshotter 中补充 restore() 方法
  → 遍历 snapshot.states → 按 WidgetId 反序列化 → 插入 StateStore
  → 应用 metadata:
      focus_path → FocusManager::focus()
      scroll_positions → 各滚动容器恢复
      current_route → 路由跳转
  → 所有 widget 重新 view() → diff → layout → paint
  → 发送 IpcMessage::Ready
```

---

## 9. 降级策略

```
级别              失败原因                 降级行为
─────────────────────────────────────────────────────
样式热重载         .rgss 语法错误           保持旧样式，显示错误信息
结构热重载         .rgui 解析失败           保持旧结构，显示错误信息
快速重启           增量编译失败             重试 cargo build（最多 3 次）
快速重启           AppProcess 启动超时(>10s) 降级为普通重启
快速重启           状态恢复失败              清空状态，从 App::new() 冷启动
普通重启           二进制崩溃                DisplayProcess 显示错误画面
                                              + "状态已保存到 xxx" 提示
IPC 通信           IPC 断连                  DisplayProcess 显示重连动画
                                              自动重连（最多 5 次）
```

---

## 10. 脚本热重载（预留）

阶段 2 将引入 Rhai 脚本引擎。脚本热重载是本系统的第 4 层反馈：

### 10.1 流程

```
.rhai 文件变更 → notify 检测 → Rhai 重新编译 → 替换命令处理器 → 保留业务状态 → < 500ms
```

### 10.2 App 集成 API（阶段 2 实现）

`App` 通过 `CommandRegistry` 将事件路由到 Rhai 函数。

#### AppConfig 新增字段

```rust
pub struct AppConfig {
    // ... 现有字段 ...
    /// Rhai 脚本文件路径列表。
    /// 启动时编译全部脚本，文件变更时热重载。
    pub rhai_paths: Vec<PathBuf>,
}
```

#### App 新增方法

```rust
impl App {
    /// 加载 `.rhai` 脚本并启动热重载。
    ///
    /// 内部创建 `CommandRegistry`（共享引用）和 `RhaiHotReload`。
    /// 启动时编译全部脚本，每帧轮询文件变更并自动重新注册。
    ///
    /// 脚本编译失败时保留旧处理器，通过 stderr 报告错误（D7 §9 降级策略）。
    pub fn load_rhai_scripts(&mut self, paths: &[impl AsRef<Path>]) -> Result<(), RhaiHotReloadError>;

    /// 获取共享的 `CommandRegistry`（用于注册 Rust 端类型和函数）。
    ///
    /// 在 `load_rhai_scripts` 之后调用，向引擎注册自定义类型：
    ///
    /// ```ignore
    /// app.load_rhai_scripts(&["scripts/handlers.rhai"])?;
    /// app.command_registry().unwrap().lock().unwrap()
    ///     .engine_mut().register_type::<MyState>();
    /// ```
    ///
    /// 返回 `None` 如果未调用 `load_rhai_scripts`。
    pub fn command_registry(&self) -> Option<&CommandRegistry>;
}
```

#### 事件路由——`handle_click` 集成

点击事件路由优先级（从高到低）：

1. **WidgetSpec 实例处理器**（现有，`widget_instances`）——组件内部 update()
2. **Rhai 命令处理器**（[V5] 新增）——`command_registry.call_fn(action, ())`
3. **旧 register_interaction 回调**（现有 fallback）

```rust
// handle_click() 新增分支（伪代码，在现有 WidgetSpec 处理器和 fallback 回调之间插入）
if let Some(ref registry) = self.command_registry {
    let action = ...; // 从 interactions 映射获取
    match registry.call_fn::<()>(&action, ()) {
        Ok(_) => return,  // Rhai 函数执行成功，事件已消费
        Err(_) => { /* Rhai 无此函数，继续传播 */ }
    }
}
// 继续现有 fallback 路径
```

#### AppHandler 集成点

1. **启动时**（`resumed()`）：
   - 若 `config.rhai_paths` 非空，创建 `CommandRegistry` + `RhaiHotReload`
   - 编译全部脚本，注册到 `CommandRegistry`

2. **每帧渲染前**（`RedrawRequested`）：
   - `RhaiHotReload.check_and_reload()` → 若有变更自动重新编译

3. **事件分发**（`handle_click`）：
   - 在现有 WidgetSpec 处理器之后、旧回调之前插入 Rhai 路由

4. **降级策略**（D7 §9）：
   - `.rhai` 编译失败 → 保留旧 AST，stderr 输出错误
   - 函数调用 panic → `catch_unwind` 隔离，stderr 报告，不崩溃

#### 依赖关系

```
rgui (facade) ──→ rgui-devtools (feature: devtools)
rgui-devtools ──→ rgui-script (已添加)
rgui (facade) ──→ rgui-script (新增)  ← 直接依赖，非 optional
```

> **为什么 rgui 直接依赖 rgui-script（非 optional）**：`handle_click` 中的事件路由需要 `CommandRegistry` 类型。若使用 feature gate，需在 `handle_click` 中 `#[cfg(feature = "devtools")]` 分支——导致非 devtools 构建无法使用脚本层。考虑到脚本层是阶段 2 核心特性，设计为常驻依赖。

#### 用户使用示例

```rust
use rgui::app::{App, AppConfig};
use rgui::AppMessage;

#[derive(Debug, Clone, PartialEq, AppMessage)]
enum Msg { Clicked }

fn main() -> Result<(), Box<dyn std::error::Error>> {
    let config = AppConfig::new()
        .title("rgui App")
        .rgui_path("ui/app.rgui")
        .rhai_paths(vec!["scripts/handlers.rhai".into()]);  // [V5] 新增

    let mut app = App::new(config);
    app.register_defaults();
    app.load_rhai_scripts(&["scripts/handlers.rhai"])?;  // [V5] 新增
    app.load_rgui::<Msg>("ui/app.rgui")?;
    app.run()
}
```

---

## 11. 边界情况处理

| 情况 | 处理 |
|------|------|
| 热重载期间用户操作 | 事件排队，热重载完成后批量处理 |
| 快照序列化失败 | 记录错误，跳过本次快照，下次重试 |
| AppProcess 版本不兼容 | framework_version 比对，不匹配时全量重建 |
| 连续保存（debounce） | 合并 300ms 内多次保存为一次重载 |
| 快照 > 100MB | zstd 压缩 + 分块传输；延迟恢复超大状态 |
| DisplayProcess 崩溃 | AppProcess 退出；用户重新启动 |

---

## 12. 验证标准

### 单元测试

| 验证项 | 预期结果 |
|--------|---------|
| IpcMessage 序列化/反序列化（所有变体） | 正确往返 |
| Snapshot 序列化（100 widget） | < 50ms |
| 降级决策（模拟失败场景） | 正确降级 |

### 集成测试

| 验证项 | 测试方法 | 预期结果 |
|--------|---------|---------|
| 样式热重载 | 修改 .rgss 文件 | < 200ms 生效 |
| 快速重启（状态恢复） | 修改 .rs → 重启 → 验证状态 | 状态不丢失 |
| 焦点恢复 | TextField 有焦点，重启 | 焦点恢复 |
| 降级到普通重启 | 模拟恢复失败 | 冷启动但不 crash |
| Debounce | 300ms 内保存 3 次 | 只触发 1 次重载 |

### D0 不变式验证

- [ ] 不变式 1：rgui-devtools 不依赖 wgpu/winit
- [ ] 不变式 2：快照仅包含 PersistState，无 GPU 资源

---

> **下一步：** D1-D7 子系统设计全部完成。进入第三层文档：D8（阶段 0 开发任务分解）、D9（测试策略与基础设施设计）、D10（组件开发规范与示例）。
