# R20 完工总结

> 日期：2026-06-16 | 任务：R20 字形 Atlas 纹理渲染（DrawGlyphs → Vello draw_image）

## 修改清单

| 文件 | 变更 |
|------|------|
| `rgui-render/src/glyph.rs` | GlyphAtlas 新增 `pixels: Vec<u8>` CPU 端 RGBA8 像素缓冲区，字形光栅化时同步复制 |
| `rgui-render/src/text_renderer.rs` | 新增 `atlas_dimensions()`/`atlas_pixels()`/`is_dirty()`/`clear_dirty()`，修复 atlas_w/h 重复乘 atlas 尺寸的 bug |
| `rgui-render/src/text.rs` | TextEngine 改用 FontSystem::new() 加载系统字体 + 嵌入字体（CJK 支持） |
| `rgui-render/src/vello.rs` | VelloBackend 新增 atlas_cache + set_atlas_data()；encode_scene_to_vello 接收 atlas_data；DrawGlyphs 用 peniko::ImageData + Brush::Image 渲染真实字形；无 atlas 时回退占位色块 |
| `rgui/src/app.rs` | 渲染循环中检测 text_renderer.is_dirty() → ctx.set_atlas_data() |
| `examples/one-button/` | 新建单按钮示例 |
| `docs/D8-阶段0开发任务分解.md` | 新增 R20 任务 |
| `docs/D12-进度跟踪.md` | 新增 R20 跟踪行 + 汇总更新 |

## 验证

- `cargo build` ✅
- `cargo test`（159 测试）✅
- `cargo clippy -- -D warnings` ✅
- `cargo fmt` ✅
- `cargo run -p counter` ✅
