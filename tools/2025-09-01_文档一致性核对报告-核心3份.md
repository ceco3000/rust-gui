# 文档一致性核对报告（核心 3 份：D0 / D11 / CLAUDE.md）

> 核对方：devco-reviewer｜日期：D11 文档同步铁律执行
> 基准：实际代码（rgui-core/src/traits.rs、各 Cargo.toml、examples/window_demo.rs、rgui-macros/src/lib.rs、核心模块文件清单）
> 方法：逐份 read_file 对照代码实况；`cargo check --workspace`（无 feature）实跑验证编译结论
> 结论格式：每份文档 × 是否一致 × 不符点（点名文档未同步条目）

---

## 〇、总览

| 文档 | 一致性 | 严重度 |
|---|---|---|
| **CLAUDE.md** | ✅ **一致**（含少量描述性细节需留意，见 §三） | 通过 |
| **D0-总体设计.md** | ❌ **不一致（多处，重点是"D3 阶段 0"定性与现状脱节）** | **P1（放行前需 doc 同步）** |
| **D11-Cargo结构与发布策略.md** | ❌ **不一致（platform default、编译结论过时）** | **P1（放行前需 doc 同步）** |

**MERGE GATE 建议：核心 3 份未全一致，暂不放行。** CLAUDE.md 通过；D0 与 D11 存在需 doc 修正的不符点，修正后再放行。

---

## 一、CLAUDE.md —— ✅ 一致（放行）

### 逐项核对
| CLAUDE.md 条款 | 实况（代码） | 结果 |
|---|---|---|
| 5-crate 列表（core/render/platform/macros/facade）| Cargo.toml members 恰好 5 个，职责对齐 | ✅ |
| `rgui-core` 纯 Rust 零 GPU/平台 | core Cargo.toml 无 wgpu/vello/winit/cosmic；源码零引用 | ✅ |
| `rgui-platform` 依赖 winit **默认启用** `default=["winit"]`（CLAUDE.md:17）| platform Cargo.toml `default=["winit"]` | ✅ |
| 依赖方向 render/platform/macros 只向下依赖 core、互不相依 | D8/D9 已验证 | ✅ |
| 命令：`cargo check --workspace` / `--features window` / `test --workspace --all-features` / `fmt --check` | 全部有效（实测） | ✅ |
| `cargo run -p rgui --features window --example window_demo`（Accordion，点击/Space 切换；`-- --expanded` 初始展开）| window_demo.rs:34-35 确认处理 `--expanded` | ✅ |
| 四核心 trait（WidgetSpec/AppMessage/PersistState/EventResult）| traits.rs 签名一致 | ✅ |
| Tier 1 单一路径、弃 .rgui/.rhai/Rhai/devtools/skia | 现状一致 | ✅ |
| 文档同步铁律、TDD、防火墙、lint 克制 | 与现状一致 | ✅ |

### 留意项（非不符，属"描述性精确度"）
- CLAUDE.md:16 说 render"**cosmic-text 字形**"已实现——属实（D9 draw_glyphs），CLAUDE.md 走在最前，准确。
- CLAUDE.md:22 说"render/platform 互不相依（DAG 无环）"——属实（D8 验证）。

**结论：CLAUDE.md 与代码一致，通过。**（它是三份中最准确、最贴近 D10 现状的一份——应为总监通道最新写入所致。）

---

## 二、D0-总体设计.md —— ❌ 不一致（P1，需 doc 同步）

### 不符点 1【最严重】：通篇定性"当前处于 D3 阶段 0（可编译骨架+契约占位）"，与代码现状（D10）严重脱节
- D0:6 声明"当前处于 D3 阶段 0"；D0:216 标题"当前实现状态（D3 阶段 0）"；D0:236 表格"热重载 P1 再议（阶段 0 不做）"。
- **代码实况（D10）**：真实 Accordion/WaBadge 组件（components.rs）、cosmic-text 真实字形（text.rs draw_glyphs）、winit 事件循环 + 按需重绘（platform event_loop.rs）、App::run 统一入口（rgui/src/app.rs）、真实窗口渲染（截图实证）。**早已远超"D3 骨架占位"。**
- **判定**：D0 的基线锁死在 D3，与代码大幅脱节。**"写实"标注本身写实，但写的是 3 个阶段前的旧实况**。doc 需更新为 D10 现状。

### 不符点 2【文档未同步】：D0 §7.3/§9.3 说"platform 须启用 winit feature 才编译 / `cargo check --workspace` 无 feature 会失败"
- **代码实况**：`rgui-platform/Cargo.toml` `default=["winit"]`（默认启用）。实测 `cargo check --workspace`（无 feature）**通过**（`Finished dev profile`，仅 core 2 个 dead_code warning），**未失败**。
- **判定**：D0 §7.3"须启用 winit feature 才能编译"、D0 §9.3"无 feature 会在 platform 因缺 winit 失败"——**与代码冲突**。这是 D8 之后（platform winit 改默认启用）被推翻的**旧陈述，文档未同步**。

### 不符点 3【文档错误】：D0 §9.4 声称 `rgui-macros` 有 `tests/widget_spec_derive_test.rs`、`tests/html_macro_test.rs`
- **代码实况**：`rgui-macros/tests/` 目录**不存在**（ls 空）。这两个测试文件**不存在**。
- **判定**：D0 声称的测试文件不存在，属**文档错误**（很可能沿用了旧项目结构）。doc 需修正（删除或改为实际存在的测试）。

### 相符点（如实反映代码现状，确认属实）
- D0 §5 类型表：`Color` 用 `u8`（非 greenfield 的 f32）、`PropValue` 无 `WidgetId` 变体、`WidgetView<M=()>` 含 `size: Option<Size>`、`Key {Str, Num}`——**全部与实际代码一致**（view.rs/color.rs 核实）。这些是 doc 如实反映代码现状，**不构成文档↔代码不一致**，是正确写实。✓
- D0 §4.3 `WidgetSpec` 签名（measure(BoxConstraints,&MeasureContext)/accessibility() 默认 none）——**与 traits.rs 一致**，且 D3 的契约漂移已修复正确。✓
- D0 §4.4 `EventResult{Handled,Prevented,Continue(M)}` + 派生 Debug/Clone/PartialEq/Eq——**与 traits.rs 一致**（派生齐全属实）。✓
- D0 §7.1 core 17 模块、§7.2 render、§7.5 facade 12 模块——**与模块文件清单一致**（全部存在）。✓
- D0 §7.4 macros 四宏透传（无 syn/quote）——**属实**（macros/lib.rs 四个 `{input}` 透传入口）。✓
- D0 §8 硬约束表（A 防火墙/C proc-macro/D lint 克制/单一 vello/唯一 Tier1/devtools 删/删 AccessKit 桥）——**与代码一致**。✓
- D0 §4.2 PersistState 无 `erased_serde::Serialize`（序列化待 D6/D7）——**属实**（traits.rs 当前仅 Any 上下转型）。✓

---

## 三、D11-Cargo结构与发布策略.md —— ❌ 不一致（P1，需 doc 同步）

### 不符点 1【文档未同步】：D11 §3 feature 表 `rgui-platform` 默认 feature 标为 `[]`，但代码是 `default=["winit"]`
- D11 §3 表格：`rgui-platform` 默认 feature = `[]`，`winit = ["dep:winit","dep:raw-window-handle","rgui-core/layout"]`。
- **代码实况**：platform Cargo.toml `default=["winit"]`（line 19）——**默认启用 winit**。
- **判定**：D11 §3 默认 feature 列 `[]` **与代码不符**。且 D11 §3 的 `winit` feature 含 `raw-window-handle`——**与代码一致**（platform Cargo.toml 有 raw-window-handle optional）✓，但默认值错。

### 不符点 2【文档未同步】：D11 §4 命令表 `cargo check --workspace` 标 ⚠️ **失败**（"platform 缺 winit 无法编译"）
- **代码实况**：实测 `cargo check --workspace`（无 feature）**通过**（仅 2 个 core dead_code warning，非失败）。
- **判定**：D11 §4 `cargo check --workspace = ⚠️ 失败` **陈述过时且错误**——platform `default=["winit"]` 无 feature 也能编译。**文档未同步。**

### 不符点 3【文档错误】：D11 §5.5 MSRV 定性与 D0 声明矛盾（轻微）
- D11 §5.5：`rust-version = "1.85"`（实际）——**与 Cargo.toml `rust-version = "1.85"` 一致** ✓。此点正确（D11 说了"实际"，与代码一致）。**非不符**。
- 但 D11 §1 要点也写 `rust-version = "1.85"`，一致。✓

### 相符点（确认属实）
- D11 §1 workspace 配置（resolver=2、members 5 个、workspace.package、lints.rust unsafe_code=deny、lints.clippy 空）——**与根 Cargo.toml 完全一致**。✓
- D11 §2 CRATE 清单与内部依赖（core 零依赖、render 依赖 core、platform 依赖 core、macros 无、facade 依赖全部）——**与各 Cargo.toml 一致**。✓
- D11 §2 重型外部依赖 feature 门控列（wgpu/vello/cosmic/fontdb/skrifa/pollster/winit/raw-window-handle）——**与代码一致**。✓
- D11 §3 render/macros/facade feature——**一致**（render default=[], vello-backend 六依赖；facade window 透传 vello-backend+winit）。✓
- D11 §4 `cargo check -p rgui-platform --features winit = ✅`、`cargo check --workspace --features window = ✅`——**与代码一致**（实测）。✓
- D11 §5 发布策略（0.x semver、license、MSRV 1.85）——**合理且与代码倾向一致**。✓

---

## 四、MERGE GATE 结论

**判定：核心 3 份文档未全员一致 → 暂不放行。**

| 文档 | 结论 | 处置 |
|---|---|---|
| **CLAUDE.md** | ✅ 一致 | **放行级通过**（最准确，贴近 D10 现状） |
| **D0-总体设计.md** | ❌ 不一致 | **需 doc 同步**：① 段落/标题"当前处于 D3 阶段 0"改为 D10 现状（真实组件/字形/事件循环/App::run 已实现）；② §7.3/§9.3 "platform 须启用 winit feature / 无 feature 编译失败"修正为"platform default=[winit]，无 feature 也编译通过"；③ §9.4 删除不存在的 `tests/widget_spec_derive_test.rs`/`html_macro_test.rs` 描述 |
| **D11-Cargo结构与发布策略.md** | ❌ 不一致 | **需 doc 同步**：① §3 feature 表 `rgui-platform` 默认 feature 由 `[]` 改为 `["winit"]`；② §4 `cargo check --workspace` 由 ⚠️失败改为 ✅通过（仅 core 2 个 dead_code warning） |

**符合点（确认属实的"写实差异"，不构成不一致）**：Color=u8、PropValue 无 WidgetId、WidgetView 含 size、四 trait 签名、EventResult 派生、macros 透传、模块边界清单、依赖 DAG——doc 如实反映代码现状，这些是**正确的写实**，无需整改。

**聚焦建议**：D0/D11 的"写实"对 D3 的基线准确，但**对 D8 之后的 platform-default-winit 改动和 D10 的组件/字形/事件循环演进未同步**。**CLAUDE.md（总监最新写入）已经准确**，说明差异集中在 doc 建立的 D0/D11 两份。建议 doc 参照 CLAUDE.md 已同步的事实（尤其 platform default=["winit"]），快速回填 D0/D11 的 4 个不符点，即可通过本 3 份核对。

---

*核对方：devco-reviewer｜只读核对，未改任何文档/代码；`cargo check --workspace` 为只读编译验证（仅产生 target 产物）。核心 3 份中 CLAUDE.md 通过，D0/D11 需 doc 回填 4 个不符点后方可放行。*
