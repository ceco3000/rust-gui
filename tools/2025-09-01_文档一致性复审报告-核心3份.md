# 文档一致性复审报告（核心 3 份 · 回填复核）

> 核对方：devco-reviewer｜复审对象：docs/D0-总体设计.md + docs/D11-Cargo结构与发布策略.md + CLAUDE.md
> 基准：实际代码（traits.rs、各 Cargo.toml、examples/window_demo.rs、rgui-macros/src/lib.rs、模块文件清单）
> 方法：逐份 read_file 对照代码实况；本轮聚焦 doc 已回填的 5 个不符点是否闭环 + 有无残留偏差
> 注：greenfield 蓝图由 architect 按方向 A 更新中，本轮以"代码 + 文档互洽"为准，暂不以其为基准

---

## 〇、复审结论

| 文档 | 一致性 | 结论 |
|---|---|---|
| **CLAUDE.md** | ✅ **一致** | 通过（上轮已确认，未变） |
| **D0-总体设计.md** | ✅ **一致（5 个不符点全闭环）** | **放行级通过** |
| **D11-Cargo结构与发布策略.md** | ✅ **一致（2 个不符点全闭环）** | **放行级通过** |

**MERGE GATE：核心 3 份文档与代码一致 → 放行。**

---

## 一、D0 回填复核（5 个不符点 → 全部闭环）

| # | 原不符点 | 回填后 | 代码核实 | 结果 |
|---|---|---|---|---|
| 1 | 通篇"D3 阶段 0"定性 | D0:6 "当前为 D10 现状（核心组件/字形/事件循环/App::run 已实现）"；D0:216 标题"D10 现状"；D0:218 列出已实现项 | components.rs、text.rs、event_loop.rs、app.rs **全部存在** | ✅ 闭环 |
| 2 | §7.3/§9.3 "platform 须启用 winit / 无 feature 失败" | D0:175 "winit 已为 platform 默认启用（default=["winit"]，非可选）"；D0:220 "cargo check --workspace 默认通过" | platform Cargo.toml `default=["winit"]`；实测 check --workspace 通过 | ✅ 闭环 |
| 3 | §9.4 宏测试误引（widget_spec_derive_test 等不存在） | D0:221 "rgui-macros 无集成测试（宏仍为透传契约，无 tests/ 目录）" | rgui-macros/tests/ **不存在**；macros/lib.rs 四宏透传 | ✅ 闭环 |
| 4 | §5 正确写实（Color=u8、PropValue 无 WidgetId 等） | **保留未动** | view.rs/color.rs 属实（正确写实） | ✅ 保留正确 |
| 5 | §7.4 macros 透传 | **保留** | macros/lib.rs 四个 `{input}` 透传入口 | ✅ 保留正确 |

### D0 复核中确认的其它相符点（无需整改）
- §4 四 trait 签名（measure(BoxConstraints,&MeasureContext)/EventResult{Handled,Prevented,Continue(M)} 含派生）——与 traits.rs 一致。✓
- §2 5-crate 拓扑、§3 依赖 DAG、§8 硬约束表（A/C/D/单一vello/唯一Tier1/devtools删/AccessKit桥删）——与代码一致。✓
- §7.1 core 17 模块、§7.2 render、§7.5 facade 12 模块边界清单——与模块文件清单一致。✓
- §4.2 PersistState 无 erased_serde（序列化待补）——属实。✓

---

## 二、D11 回填复核（2 个不符点 → 全部闭环）

| # | 原不符点 | 回填后 | 代码核实 | 结果 |
|---|---|---|---|---|
| 1 | §3 platform 默认 feature 标 `[]` | D11:62 `rgui-platform` 默认 feature = `["winit"]`；D11:48 注明"winit 默认启用" | platform Cargo.toml `default=["winit"]` | ✅ 闭环 |
| 2 | §4 check --workspace 标 ⚠️失败 | D11:82 `cargo check --workspace` = ✅ 通过（platform default=[winit] 默认构建通过） | 实测通过（仅 core 2 个 dead_code warning，非失败） | ✅ 闭环 |

### D11 复核中确认的其它相符点（无需整改）
- §1 workspace 配置（resolver=2、members 5 个、lints.rust unsafe_code=deny）——与根 Cargo.toml 一致。✓
- §2 CRATE 清单与依赖（core 零依赖/render 依赖 core/platform 依赖 core/macros 无/facade 全）——一致。✓
- §2/§3 重型外部依赖 feature 门控列（vello/wgpu/cosmic/fontdb/skrifa/pollster/winit/**raw-window-handle**）——raw-window-handle 在 platform Cargo.toml（optional, line 15）✓；core `default=["layout"]`（Cargo.toml:16）✓。
- §4 `cargo check -p rgui-platform --features winit`、`check --workspace --features window` 均 ✅——与代码/实测一致。✓
- §5 发布策略（0.x semver、license、MSRV 1.85）——合理。✓

---

## 三、CLAUDE.md（未变，上轮已确认一致）

- 5-crate 列表、platform winit 默认启用（CLAUDE.md:17）、命令（含 window_demo 的 `--expanded`，代码 window_demo.rs:34-36 存在）、四 trait、Tier1 单一路径、文档铁律/防火墙/TDD/lint 克制——全部与代码一致。✓
- **本轮复核 window_demo.rs 确认**：含 Accordion 组件 + `--expanded` 参数处理（window_demo.rs:1/9/34-36）——CLAUDE.md 描述准确。✓

---

## 四、MERGE GATE 判定

**核心 3 份文档（D0/D11/CLAUDE.md）与代码一致 → 放行（PASS）。**

- doc 回填的 5 个不符点（D0 的 3 个 + D11 的 2 个）**全部闭环**，且回填细节（D0:218 已实现项、D0:175/220 winit 默认启用、D0:221 宏无测试、D11:62/82 platform default=[winit]/check 通过）均与代码实况逐条核实一致。
- 上轮确认的"正确写实无需改"部分（Color=u8、PropValue 无 WidgetId、四 trait 签名、模块边界、依赖 DAG、宏透传）**全部保留且仍正确**。
- CLAUDE.md 未变，继续通过。

**放行。** greenfield 蓝图（architect 按方向 A 更新）完成后，我按同样标准对 greenfield 做一次与代码的核对（8 处差异 + 是否与 D0/D11/CLAUDE 互洽），确保全框架文档一致后再收尾。

---

*核对方：devco-reviewer｜只读核对，未改任何文档/代码。核心 3 份（D0/D11/CLAUDE.md）已与代码一致，MERGE GATE 放行。greenfield 蓝图待 architect 更新后另行核对。*
