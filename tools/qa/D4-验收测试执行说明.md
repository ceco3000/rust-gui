# D4 验收测试执行说明 · rgui 核心实现

> 准备方：devco-qa　基准：`tools/2025-09-01_rgui-greenfield-architecture.md` §B.1 + `tasks.md` D4
> 状态：**测试用例清单 + 骨架已就绪；当前 dev 正在并行实现 state/layout（TDD RED→GREEN），故未注入。**

---

## 1. 已交付文件（tools/qa/）

| 文件 | 内容 | 注入时机 |
|------|------|---------|
| `D4-验收测试清单.md` | 三类测试完整用例（W/S/R/N + P/A/SN + L/CE + 边界），含判据 | 主交付物（规格） |
| `d4_tests/d4_acceptance_core.rs` | 组件循环 + registry + 几何/契约测试 | **可立即注入**（基于已实现 API） |
| `d4_tests/d4_acceptance_state.rs` | diff/apply_patch/snapshot | **待 dev 实现 state::diff/apply_patch/Patch::SetProps** |
| `d4_tests/d4_acceptance_layout.rs` | LayoutStyle→Taffy Style + LayoutEngine 计算 | **待 dev 实现 to_taffy_style/LayoutStyle/compute_layout** |

---

## 2. 当前 D4 进度核验（2025-09-04 只读快照）

| D4 目标 | 验收用例域 | dev 现状 | 我的骨架可用性 |
|---------|-----------|---------|---------------|
| 1. WidgetSpec 核心循环（Coordinator） | W/S/R/N | `coordinator.rs` 已实现（`Coordinator::new/current_view/dispatch/name/state`），`core_loop.rs` 3 测试 **GREEN** | ✅ core.rs 可注入 |
| 2. state diff/snapshot | P/A/SN | `state/` 拆分为 `diff.rs`/`snapshot.rs`（16:14 新建），`state.rs` 已另存为目录；`diff/apply_patch/Patch::SetProps` 待实现 | ⏳ state.rs 待 dev 落地后注入 |
| 3. 布局 Taffy 集成 | L/CE | `layout.rs` 仍占位（无 `to_taffy_style`/`LayoutStyle`/`compute_layout`） | ⏳ layout.rs 待 dev 落地后注入 |

> ⚠️ dev 正在并行编辑 `state/`/`layout.rs`——**QA 勿在 dev 编码期间对 core/state/layout 做写/编译动作**，避免污染半成品或误判 RED。

---

## 3. 注入步骤（等 dev 交付 + 总监通知后执行）

```bash
# 1) 组件循环 + 契约（已实现 API，可先行）
cp tools/qa/d4_tests/d4_acceptance_core.rs rgui-core/tests/
cargo test -p rgui-core --test d4_acceptance_core   # 期待全绿
rm rgui-core/tests/d4_acceptance_core.rs

# 2) 状态 diff/snapshot（dev 实现后）
cp tools/qa/d4_tests/d4_acceptance_state.rs rgui-core/tests/
cargo test -p rgui-core --test d4_acceptance_state  # 期待全绿
rm rgui-core/tests/d4_acceptance_state.rs

# 3) 布局（dev 实现后）
cp tools/qa/d4_tests/d4_acceptance_layout.rs rgui-core/tests/
cargo test -p rgui-core --test d4_acceptance_layout  # 期待全绿
rm rgui-core/tests/d4_acceptance_layout.rs

# 4) 全绿回归 + 门槛
cargo test --workspace   # 不得回归 D3
cargo check --workspace  # 0 error
```

**注意**：每份骨架**注入后立即删除**（不回留 rgui-core/tests/，避免与 dev 同名测试冲突或污染交付物）。骨架文件唯一权威副本在 `tools/qa/d4_tests/`。

---

## 4. 判据与报告

- D4 通过 = 上述 1-4 全绿。
- 任一失败/编译失败 → 退回 dev，附失败样例（哪个 test 文件、断言、输入、expected vs actual）。
- 关注点：注入时若遇 API 签名与骨架不符（如 `Patch` 变体、`LayoutStyle` 字段名），**以 dev 实际契约 + greenfield §B.1 为唯一基准**微调骨架后重验，并向总监报偏差。

---

## 5. 备注：QA 边界纪律

- 不在 dev 编码期间对 `core/state/layout/coordinator` 做任何写或编译（避免撞半成品）。
- 每次注入骨架 = cp → test → rm，绝不留残留（已发生一次 `d4_acceptance_state.rs` 残留，已清理，教训记入）。
- 我这边的产物只在 `tools/qa/`（清单 md + 骨架 rs + 本说明），不进入 workspace。
