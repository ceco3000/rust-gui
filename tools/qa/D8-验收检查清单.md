# D8 验收检查清单 · 收敛窗口逻辑进 rgui-platform

> 准备方：devco-qa　基准：`tools/2025-09-01_rgui-greenfield-architecture.md` §B.3/§C.3/§C.4 + `tasks.md` D8
> 状态：**清单已就绪；dev 尚未交付 D8（window_demo 仍直接 winit::/wgpu::，platform 公共 API 待补全）**
> 执行：`bash tools/qa/d8_acceptance.sh` + 人工核对清单 + 截图回归（复用 capture_window.sh）

---

## 0. D8 验收目标（tasks.md D8 §141-156）

把 window_demo 直接 winit/wgpu 的**窗口创建 + 事件循环 + surface 渲染**收敛进 `rgui-platform`（+ render），让 facade/示例走 platform 公共 API 而非绕过。补齐 greenfield §C.3。

---

## 1. 逐项检查点

| # | 验收点 | 检测方式 | PASS 判据 |
|---|--------|---------|----------|
| 1 | **window_demo/facade 无直接 winit::/wgpu:: 引用** | `grep -rnE 'winit::|wgpu::' rgui rgui/examples/window_demo.rs` | **仅** platform/render 内部可含 winit/wgpu；window_demo + facade 源码 0 命中（注释除外） |
| 2 | **platform 提供 window+event_loop 封装公共 API** | 读 `rgui-platform/src/window.rs`/`event_loop.rs` + `lib.rs` 导出 | 提供 `Window` 创建（`create_window`/`WindowConfig→Attribute`）、`EventLoop`（`build`/`run`/`run_app`）、`resize`/`close` 生命周期公共 API；**不再只是"ApplicationHandler 组装在 facade"的占位注释** |
| 3 | **窗口仍稳定渲染（首帧不回归）** | 运行 window_demo + 截图（capture_window.sh）+ vision 确认蓝色按钮 | 窗口弹出即渲染蓝色按钮组件（首帧非空白，D7 修复不回退） |
| 4 | **core 零 GPU/平台防火墙** | `rgui-core/Cargo.toml [dependencies]` | 无 wgpu/vello/winit/cosmic-text/fontdb；core 源码无 rgui_render/rgui_platform |
| 5 | **DAG 无环** | `cargo tree --workspace` | 无 cycle；render/platform 互不相依，均只向下依赖 core |
| 6 | **编译通过** | `cargo check --workspace`（含 `--features window`） | 0 error |

---

## 2. platform 公共 API 契约核对表（检查点②细项）

| API | greenfield §B.3/§C.3 期望 | 现状 |
|-----|--------------------------|------|
| `window` 模块 | `Window` 封装（含 create/配置） | `WindowConfig/WindowHandle/to_winit_attributes/Window 别名` ✅ 部分 |
| `event_loop` 模块 | `EventLoop` 封装（驱动事件循环） | `EventLoop 别名`/`build()` ✅ 部分；**缺 run/run_app/ApplicationHandler 驱动封装** ⚠️ |
| `resize`/`close` | 窗口生命周期 | **缺**（待 D8 补） |
| 渲染 surface 收敛 | → `rgui-render::vello`（create_surface/resize/present） | `create_surface` 已有（render）；`resize`/`present` 封装待确认 |

> D8 关键：window_demo 的直接 winit ApplicationHandler / wgpu surface 配置 → 应收敛到 platform（窗口驱动）+ render（surface）。验收核对 window_demo 是否变为"纯 platform + render 公共 API 调用"。

---

## 3. 截图回归（检查点③）

复用 `tools/qa/capture_window.sh`：
```bash
cd /Users/chenchao/Documents/code/rust/RUST-GUI
# 后台运行（Hermes background）cargo run -p rgui --features window --example window_demo
bash tools/qa/capture_window.sh d8_01_window_initial --wait=3   # 窗口首帧
# vision 确认蓝色按钮组件可见（首帧非空白）
```
> 注意：D7 修的是"首帧时序"（ControlFlow::Poll + about_to_wait 每帧 request_redraw）。D8 收敛后必须确认**首帧即渲染**（窗口弹出即见组件，不依赖 focus），与 D7 修复不回退。

---

## 4. 执行步骤

```bash
# 静态检查（grep winit/wgpu + 编译 + 防火墙）
bash tools/qa/d8_acceptance.sh

# API 契约人工核对（检查点②，读 platform window.rs/event_loop.rs）
# 截图回归（检查点③）：运行 window_demo + capture_window.sh + vision
```

---

## 5. 判据与上报

- D8 通过 = 检查点 1-6 全部 P0 达标（尤其① window_demo 收敛 ② platform 完整 API ③ 首帧截图回归）。
- **上报项**：若 window_demo 仍直接 winit/wgpu（检查点①失败）→ 退回 dev；若 platform 仍"ApplicationHandler 在 facade"占位（检查点②失败）→ 契约名存实亡，需补完整封装。
- 截图回归结果如实记录（首帧是否稳定）。

---

## 6. QA 边界纪律

- grep/编译/截图均只读，**不改 dev 源码**。
- 截图存 `tools/qa/d7_screenshots/`（D7 已建）或复用 d7 目录，不改动 platform/render/core。
- dev 编码期间不注入测试到 platform/render 源码。
