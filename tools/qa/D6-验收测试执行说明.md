# D6 验收测试执行说明 · 真实 from_view 转换

> 准备方：devco-qa　基准：greenfield §B.2/§C.2/§C.1 + tasks.md D6
> 状态：**用例清单 + 骨架已就绪，并已验证与 dev 当前 from_view 契约对齐（A 层 8 测试全绿）。dev 的 from_view 已实现真实转换（布局+Props+文本），但 API 仍在演化（2参→1参）。**

---

## 1. 已交付（tools/qa/）

| 文件 | 内容 | 状态 |
|------|------|------|
| `D6-验收测试清单.md` | 7 节用例规格（完整 Props/布局 bounds/端到端像素/防火墙/执行/判据） | 主交付物 |
| `d6_acceptance_from_view.rs` | from_view 完整 Props + 布局 bounds + 递归坐标（A 层无 GPU） | ✅ **已验证 8/8 全绿**（对齐 dev 单参契约） |
| `d6_acceptance_e2e.rs` | 真实 WidgetView 端到端像素级（B 层 GPU） | 待 GPU 环境，单参契约已对齐 |

---

## 2. 当前 D6 进度核验（只读快照）

| 项 | dev 现状 |
|----|---------|
| `from_view<M>(&WidgetView<M>)` | ✅ **单参**（容器 = 根 size 或 DEFAULT_CONTAINER=200x200） |
| Color → FillRect | ✅ 用 view.size / slot.size（真实布局） |
| Str → DrawText | ✅ 文本色灰 200（非背景，可像素验证） |
| Unit/Bool/Int/Float/WidgetId | ✅ 无图元（容器/布局提示） |
| 布局应用 | ✅ `LayoutEngine::compute_children(slot.size, &child_sizes)` 求子节点真实 bounds |
| 离屏渲染 | ✅ `VelloBackend::render_offscreen` 已实现（RGBA8 读回） |
| dev 自有测试 | `e2e_from_view.rs`（Color/Str/端到端）+ `offscreen.rs`（D5 遗留） |

---

## 3. 注入步骤（dev 交付定型后）

```bash
# A 层：from_view（无 GPU）
cp tools/qa/d6_tests/d6_acceptance_from_view.rs rgui-render/tests/
cargo test -p rgui-render --test d6_acceptance_from_view
rm rgui-render/tests/d6_acceptance_from_view.rs

# B 层：端到端像素（GPU 环境）
cp tools/qa/d6_tests/d6_acceptance_e2e.rs rgui-render/tests/
cargo test -p rgui-render --features vello-backend --test d6_acceptance_e2e
rm rgui-render/tests/d6_acceptance_e2e.rs

# 防火墙/回归
bash tools/qa/d5_acceptance.sh
cargo test --workspace --all-features
```

**⚠️ API 演化风险**：dev 的 from_view 曾在 2参(container) 与 1参(默认容器) 间切换。骨架已对齐**当前单参**；若 dev 定型后签名变化，注入前先复核签名再微调。

---

## 4. 判据与报告

- D6 通过 = from_view 完整 Props + 布局真实作用于渲染（bounds）+ 端到端像素（替换手工 rect）+ 防火墙 + 全量回归全绿。
- 任一 FAIL/编译失败 → 退回 dev 附失败样例。

---

## 5. 上报项

1. **from_view 完整 Props 落地**（Color/Str 已映射；Int/Float/WidgetId/Bool 无图元——确认"完整处理"语义，是否需尺寸提示）。
2. **布局真实作用**（`compute_children` 求真实 bounds，draw 用实际坐标——已验 fv7/fv8）。
3. **文本路径**（Str→DrawText 已接入；真实字形 glyph/shaper 是否 D6 补全，还是 D5 矩形近似继续——scene_graph 用 DrawText size 近似，注）。
4. **D5 P1-1 是否清除**（offscreen.rs 若仍用 red_filled_rect 则 P1-1 未清；若改真实 WidgetView 则清——已见 e2e_from_view.rs 用手工构造 view，需确认 offscreen.rs 是否替换）。
5. **GPU 环境依赖**（e2e 像素级测试）。

---

## 6. 纪律

- dev 正并行实现 from_view/布局/文本——QA 不在 dev 编码期间碰 render/core 源码，不注入骨架到 rgui-render/tests/。
- **⚠️ 本轮已发生 2 次骨架残留**（from_view 2参/单参不匹配 + e2e 误放 rgui-render/tests/），均已立即清理，git 无 modified。教训：注入前后必须 find 复核，原子化 cp→test→rm。
- 产物唯一权威副本在 tools/qa/。
