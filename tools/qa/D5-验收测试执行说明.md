# D5 验收测试执行说明 · rgui 渲染管线（vello 离屏集成）

> 准备方：devco-qa　基准：`tools/2025-09-01_rgui-greenfield-architecture.md` §B.2/§C.2/§D + `tasks.md` D5
> 状态：**用例清单 + 检测脚本 + 骨架已就绪（dev 已装配 vello-backend feature，但 SceneGraph 转换与离屏渲染逻辑尚未实现）**

---

## 1. 已交付（tools/qa/）

| 文件 | 内容 | 注入时机 |
|------|------|---------|
| `D5-验收测试清单.md` | 9 节用例规格 + 分层 + 边界 + 门槛 | 主交付物 |
| `d5_acceptance.sh` | feature 门控/防火墙/DAG/回归静态检测 + 反向增量指引 | **可立即跑**（dry-run 全绿 PASS=6） |
| `d5_tests/d5_acceptance_scene.rs` | SceneGraph 转换（A 层纯逻辑） | dev 实现 `from_view`/`DrawCmd` 后 |
| `d5_tests/d5_acceptance_offscreen.rs` | 离屏像素读回（B 层 GPU） | dev 实现 `render_offscreen` + GPU 环境就绪 |

---

## 2. 当前 D5 进度核验（2025-09-04 只读快照，非阻塞）

| D5 目标 | dev 现状 | 我的可用性 |
|---------|---------|-----------|
| 3. feature 门控 | ✅ 已装配 `vello-backend`，vello0.9/wgpu29/cosmic-text0.12/fontdb0.16/skrifa0.22（optional） | d5_acceptance.sh 可跑 |
| 防火墙 | ✅ core 零 GPU（FW1-FW4 PASS）+ DAG 无环 | 已核验 |
| 2. SceneGraph 转换 | ⏳ scene_graph.rs 仍占位（无 DrawCmd/from_view） | 骨架待注入 |
| 1. 离屏像素读回 | ⏳ vello.rs VelloBackend 空壳（无 render_offscreen） | 骨架待注入 |
| 4. 反向增量 | ⏳ 待 build 产物后实测（INC1） | 手动 |

---

## 3. 注入步骤（dev 交付后）

```bash
# A 层：SceneGraph 转换（dev 实现 from_view/DrawCmd 后）
cp tools/qa/d5_tests/d5_acceptance_scene.rs rgui-render/tests/
cargo test -p rgui-render --test d5_acceptance_scene
rm rgui-render/tests/d5_acceptance_scene.rs

# B 层：离屏像素读回（dev 实现 render_offscreen + GPU 环境；环境不可用则报告并 skip）
cp tools/qa/d5_tests/d5_acceptance_offscreen.rs rgui-render/tests/
cargo test -p rgui-render --features vello-backend --test d5_acceptance_offscreen
rm rgui-render/tests/d5_acceptance_offscreen.rs

# 静态门控/防火墙
bash tools/qa/d5_acceptance.sh

# 反向增量（INC1，仅改注释后还原）
# 回归门槛
cargo test --workspace --all-features   # 基线 D4 41 测试
cargo check --workspace
```

**纪律**：骨架 cp→test→rm，即取即删，唯一副本在 `tools/qa/d5_tests/`（D4 残留教训记入）。

---

## 4. 判据与报告

- D5 通过 = §2-3 全部达成（B 层在 GPU 就绪环境验证；否则报告"环境依赖"）。
- 任一 FAIL/编译失败 → 退回 dev，附失败样例（test 文件、断言、输入、expected vs actual）。

---

## 5. 上报项（QA 视角）

1. **core `default=["layout"]` 与 greenfield §D `default=[]` 偏差（P2，tasks.md:12 已记）**：taffy 纯 Rust 布局，**不违反**"零 GPU/零平台"防火墙语义，但违反"default 为空、重型依赖隔离"的 feature 原则。建议 D5 由总监拍板：core default 改 `[]`，或明确接受。QA 未擅自改。
2. **vello 版本并存风险**：cargo 缓存同时有 vello 0.8 与 0.9。dev 的 render 已锁 0.9（`vello = "0.9"`）。需确认未引入 0.8 变体（旧项目 skia 版本冲突教训）。QA 未改 dev 依赖。
3. **离屏像素读回的环境依赖**：B 层测试依赖 wgpu/vello GPU 或软适配器；若本机不可用，将报告"环境依赖"而非 dev 缺陷。

---

## 6. QA 边界纪律（含 D4 残留教训强化）

- dev 正并行实现 render/SceneGraph/vello——QA **不在 dev 编码期间**对 render/core 做任何写或注入（避免污染半成品、误判 RED）。
- 静态脚本（d5_acceptance.sh）只读，可随时跑。
- 产物唯一权威副本在 tools/qa/。
- **⚠️ D4 教训强化**：骨架注入必须**原子化**——"cp → test → rm"三步中，rm 可能因 dev 并行改写/时序未生效，导致残留。**改法**：
  1. 用**唯一的临时文件前缀**（如 `d4_acceptance_qa_tmp`）+ 注入后**立即用 `find` 复核无残留**；
  2. 注入测试放在**独立 staging 目录**（不在 `rgui-core/tests/` 内 cp，改用 `--test` 指向/或临时 workspace crate），避免直接碰 dev 的 tests；
  3. 每次注入前后 `git status --short` 对比，确认只增删我自己的文件。
- 已在 D4 留下 2 个残留（d4_acceptance_core/state.rs 位于 rgui-core/tests/），已清理，git 无 modified。若再发现同类污染，立即报告并暂停注入。
