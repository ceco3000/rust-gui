# D10 验收检查清单 · Accordion/WaBadge 组件 + 真实交互 demo

> 准备方：devco-qa　基准：`tools/2025-09-01_rgui-greenfield-architecture.md` §B.1 + `tasks.md` D10
> 状态：**清单已就绪；dev 尚未交付 D10（components.rs 仍为 D3 占位，Accordion/WaBadge 是空 stub）**
> 执行：dev 交付后 `bash tools/qa/d10_acceptance.sh` + 交互截图脚本（对比图）

---

## 0. D10 验收目标（tasks.md D10 §209-235）

1. **Accordion 组件**：折叠/展开容器，点击标题切换展开，子内容显示/隐藏，`impl WidgetSpec`（真实交互：点击→update→view 更新→重绘）
2. **WaBadge 组件**：徽章/标签，整数 label 显示，`impl WidgetSpec`
3. **真实交互 demo**：窗口显示 Accordion + WaBadge，点击 Accordion 标题展开/收起，文本显示状态（截图验证交互前后变化）
4. WidgetSpec 完整生命周期（view/update/measure/paint）

---

## 1. 逐项检查点

| # | 验收点 | 检测方式 | PASS 判据 |
|---|--------|---------|----------|
| 1 | **Accordion 实现** | grep `rgui-core/src/components.rs` 的 Accordion `impl WidgetSpec` | Accordion 有真实 State/Message/view（非空 stub）；update 处理"切换展开"消息；view 根据 expanded 状态显示/隐藏子内容 |
| 2 | **Accordion 展开/收起交互** | read components.rs update 逻辑 + 单测 | update(展开消息) 翻转 expanded；view 反映 expanded（子内容显示/隐藏）；有单元测试验证 |
| 3 | **WaBadge 实现** | grep WaBadge `impl WidgetSpec` | WaBadge 有 State 含整数 label；view 显示整数；update 处理"
 | message |
| 4 | **交互 demo（点击切换展开/收起）** | 运行 demo + 截图前后对比 + vision | 窗口显示 Accordion+WaBadge；点击 Accordion 标题 → 展开状态变化（截图对比图 + vision 确认） |
| 5 | **WidgetSpec 完整生命周期** | read Accordion/WaBadge 实现 | view/update/measure/paint 全实现（非空/非仅默认），且行为正确 |
| 6 | **core 零 GPU/平台；DAG 无环；单一 vello/winit** | d5_acceptance.sh 复用 | core 无 GPU/平台依赖；cargo tree 无环；无 skia 残留 |
| 7 | **cargo test --workspace --all-features 全绿** | 全量测试 | 0 failed（含组件新测试） |

---

## 2. 交互前后对比截图（检查点④核心）

**方法**：运行动画 demo（窗口显示 Accordion+WaBadge）→ 截"展开前" → 点击 Accordion 标题 → 截"展开后" → vision 对比确认状态变化。

| 截图 | 场景 | 期望 |
|------|------|------|
| `d10_01_before_click` | 初始（收起/未展开） | Accordion 收起（标题可见，子内容隐藏），WaBadge 显示整数 label |
| `d10_02_after_click` | 点击 Accordion 标题 | Accordion 展开（子内容显示），或收起状态切换 |
| `d10_03_state_detail` | 展开后（vision 聚焦） | 确认子内容/文本状态变化 |

> **关键**：截图前后对比证明"点击 Accordion 标题切换展开/收起"真实发生，非 dev 自说自话。

---

## 3. 点击自动化依赖（重要）

**自动点击**依赖辅助功能权限（CGEvent/AX）。此前 D7/D8/D9 均受限（osascript -1728、单查询模式禁脚本）。预案：
1. **优先**：若 dev 提供交互触发钩子（如键盘快捷键切换展开、或 demo 支持 CLI 参数/自动播放）→ 用键盘/钩子自动化。
2. **次选**：用户/总监授予 Hermes 宿主"辅助功能"权限 → 可用 CGEvent 坐标点击。
3. **兜底**：无法自动化点击时，截图"展开前"，然后**手动/请总监**点击后再截"展开后"，我 vision 对比。或如实说明交互自动化受限（用户已验证组件本身）。

> 清单中已注明此依赖，执行时按可用路径。

---

## 4. 执行步骤

```bash
# 静态检查
bash tools/qa/d10_acceptance.sh

# 交互 demo：运行 + 截图前后对比
cd /Users/chenchao/Documents/code/rust/RUST-GUI
# 运行动画（dev 给出命令，如 cargo run -p rgui --features window --example <demo>）
bash tools/qa/capture_window.sh d10_01_before_click --wait=1
# 点击 Accordion 标题（按可用路径：键盘钩子/CGEvent/手动）
bash tools/qa/capture_window.sh d10_02_after_click --wait=1
# vision: 前后对比 + 状态细节
```

---

## 5. 判据与上报

- D10 通过 = 检查点 1-7 全部达标（尤其④交互前后对比截图 + ⑤完整生命周期 + ⑦全量测试）。
- **上报**：若 Accordion/WaBadge 仍空 stub（view 空）→ 退回 dev；若无交互 demo/无截图对比 → 注明交互验证受限；若全量测试含组件测试失败 → 退回。
- json→

## 6. QA 边界纪律

- grep/编译/截图/单测只读，**不改 dev 源码**。
- 截图存 `tools/qa/d7_screenshots/`（沿用），交互对比图单独命名。
- dev 编码期间不注入测试到 components/render/platform 源码。
