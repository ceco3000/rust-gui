# D3 验收检查清单 · rgui 从零 scaffold（5-crate greenfield 定稿）

> 准备方：devco-qa　唯一基准：`tools/2025-09-01_rgui-greenfield-architecture.md`（用户已拍板 5-crate）
> 执行：`bash tools/qa/d3_acceptance.sh`（只读，不改 dev 结构）
> 状态：**清单已切 5-crate 基线；dry-run 待重跑；dev 交付后执行。**

---

## 0. 基线定稿（5-crate，与 6-crate 的差异已确认）

| # | 目标 crate | 纳入 | 硬约束 |
|---|-----------|------|--------|
| 1 | `rgui-core` | 现 core + **state** + **layout** + **style**(占位) + **components** + a11y 树 | 纯 Rust，零 GPU/零平台/零 cssparser（Cargo 依赖防火墙） |
| 2 | `rgui-render` | 现 render | wgpu/vello/cosmic-text 隔离；删 skia；与 platform 互不相依 |
| 3 | `rgui-platform` | 现 platform | winit 隔离；与 render 互不相依 |
| 4 | `rgui-macros` | 现 macros | proc-macro 必须独立 |
| 5 | `rgui` (薄 facade) | 现 facade 瘦身 | 依赖全部 4 个；仅装配 ≤200 行 |

**删并确认**（greenfield §A.2/F）：`rgui-style`/`rgui-state`/`rgui-layout`/`rgui-components` 并入 core；`rgui-script`/`rgui-devtools` 删除；`rgui-a11y` 降级为 core::a11y（删 AccessKit 桥）。**不出现**任何上述独立 crate。

---

## A. 逐项验收点

| # | 验收点 | 检测方式 | 判据（PASS 条件） |
|---|--------|---------|------------------|
| 1 | `cargo check --workspace` 通过 | `cargo check --workspace` | 退出码 0，无 error |
| 2 | 依赖 DAG 无环 + 防火墙 | `cargo tree --workspace` 无 cycle；core 依赖静默核对 | tree 无 `cycle`；`rgui-core/Cargo.toml` 无 render/platform/macros/winit/wgpu/vello/cosmic-text/cssparser；render 与 platform 互不相依 |
| 3 | **5 成员齐全**，无外围独立 crate | `cargo metadata` / workspace members | 恰为 5：core/render/platform/macros/rgui；**无** rgui-style/state/layout/components/script/devtools/a11y 独立成员 |
| 3b | `rgui-macros` 独立 proc-macro | 读 `rgui-macros/Cargo.toml` | `[lib] proc-macro = true` |
| 3c | core 子模块 + style 占位 | 读 `rgui-core/src/lib.rs` | `pub mod state/layout/components/a11y/style` 齐全；`core::style` 为占位（P1）且**不引 cssparser** |
| 4 | 核心 trait 契约占位签名与 **greenfield §B.1** 一致 | 提取 `traits.rs` 签名 | `AppMessage`/`PersistState`/`WidgetSpec`/`EventResult` 签名与 B.1 一致 |
| 5 | lint 克制（仅 unsafe_code=deny） | 读根/Cargo.toml/lints.toml 的 `[lints]` | 仅 `unsafe_code="deny"`；todo/expect/unwrap 放宽 warn；**无**旧式全 deny |
| 6 | 增量：改数据/状态层→不重编 render | 改 `core/src/state` 注释 → `cargo check -p rgui-render` | render 不重编；**口径 = greenfield §E.3（改数据/状态层）** |

---

## B. 人工核对项（脚本只出原材料）

1. **trait 签名**：greenfield §B.1 逐条对照 `AppMessage`/`PersistState`/`WidgetSpec`/`EventResult`。
2. **公共 API 防悬空**（greenfield B.0）：facade `lib.rs` 不再 `pub use rgui_a11y/state/layout/components/style`，仅保留 core/render/platform/macros。
3. **`html!` 宏产物**（greenfield B.4）：生成 WidgetSpec/WidgetView Rust 树则保留；生成 `.rgui` 中间表示则违规。
4. **示例**：`examples/` 为 Rust WidgetSpec（Accordion + WaBadge）实现，不用 `.rgui`/`.rhai`。

---

## C. 增量验收——操作口径（关键）

取 **greenfield §E.3** 精确口径（胜过 tasks.md 早版泛称）：

> 改**数据/状态层**（core::state + 底层 id/geometry/color/view）→ `cargo check -p rgui-render` **不重编**。
> core 含 components/style 等 UI 层，这些变更合法触发 render 重编，不纳入硬性 PASS。

验证时**只改注释/空实现**，验证后 `git checkout` 还原，不影响 dev 代码。需在有 build 产物状态手动触发。

---

## D. dry-run 结论（本轮）

| 项 | 当前状态 | 回显 |
|----|---------|------|
| workspace | 未初始化（无根 Cargo.toml、无 crate 目录） | `NOT_READY`（dev 未交付） |
| git | 旧代码已删（HEAD=`ae456fe`） | 基线与 tasks.md 状态一致 |

与 tasks.md「旧代码已删，dev 正从零 scaffold」一致，无可验收 workspace；脚本空态正确报 NOT_READY。
