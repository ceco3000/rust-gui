# D 系列（D1-D10）逐份一致性核对报告

> 核对方：devco-reviewer｜对象：docs/D1-D6 + D8-D10（D7 有意缺失待确认）+ 已核过的 D0/D11
> 基准：实际代码（traits.rs、各 Cargo.toml、diff.rs、scene_graph.rs、a11y.rs、platform/{input,focus}.rs、state 等）+ greenfield/D0
> 方法：逐份 read_file 对照代码实况；测试数字经 `cargo test` 实测核对（非只信文档）

---

## 〇、总览

| 文档 | 一致性 | 不符点 |
|---|---|---|
| D1 组件模型 | ✅ 一致 | 无 |
| D2 状态 | ✅ 一致 | 无 |
| D3 渲染 | ✅ 一致 | 无 |
| D4 样式 | ✅ 一致 | 无（P1 未实现标注属实） |
| D5 事件 | ❌ **不一致（1 处）** | §2 `InputModality` 声称 3 变体（含 Touch），代码实际 2 变体（无 Touch） |
| D6 无障碍 | ✅ 一致 | 无 |
| D7 | —— | **有意缺失合理**（devtools/热重载已删，不建空文档，判断通过） |
| D8 任务分解 | ✅ 一致 | 无 |
| D9 测试策略 | ✅ 一致（45 数字实测属实） | 无 |
| D10 组件规范 | ✅ 一致 | 无 |

**MERGE GATE：全框架文档与代码 100% 一致 → 放行（PASS）。** 无任何不符点。

> **补记（复核修正）**：初判 D5 §2 `InputModality` 为"无 Touch 的 P2 偏差"系**误判**——当时用 `grep -A5` 截断输出，只读到第 11 行 Keyboard，误以为无 Touch。经完整 `read_file` 复核 `rgui-platform/src/input.rs:6-14`：`pub enum InputModality { Pointer, Keyboard, Touch }`（第 13 行 Touch 确实存在，derive Debug/Clone/Copy/PartialEq/Eq/Default）。**D5 §2 写 `{Pointer, Keyboard, Touch}` 与代码一致，doc 写实正确，非偏差。** 此前的 P2 误判作废。

---

## 一、逐份结论

### D1 组件模型（✅ 一致）
- §2 `WidgetSpec` 五方法签名（measure(BoxConstraints,&MeasureContext)/accessibility() 默认 none）——与 traits.rs 一致。✓
- §3 关联类型约束（State: PersistState / Message: AppMessage）——一致。✓
- §5 `Coordinator` 方法（new/current_view/dispatch/name/state/current_view_default）——与 coordinator.rs 一致。✓
- §6 `WidgetRegistry::{RegistryError, WidgetRegistry}`、`register` 占位空实现（body 空）——与 registry.rs 一致（register 空 body 属实）。✓
- §7 视图/属性/上下文类型——与 view.rs/context.rs 一致。✓
- §8 内置组件（Accordion/WaBadge）——与 components.rs 一致。✓
- §9 当前实现状态（register 占位/derive 宏透传/hit-test P1）——写实准确。✓

### D2 状态（✅ 一致）
- §2 防火墙（M1 教训：state 零 GPU 引用）——仅注释提及，属实。✓
- §3 `StateStore<S = InstanceState>`（state/mod.rs:36）+ _marker(40)——与代码一致。✓
- §5 `Patch` 5 变体（SetProps/SetChildProps/ReplaceChild/InsertChild/RemoveChild，diff.rs:23-33）——**实际存在**（D4 审查后 diff.rs 已扩展为递归 5 变体）。✓
- §6 diff 算法（位置型递归、ReplaceChild/InsertChild/RemoveChild、收敛、**无 key-based**）——与 diff.rs:51-82 + 头部注释一致。✓
- §9 diff.rs 测试函数名：`roundtrip_deep_child_content_change_converges`(165)、`add_child_subtree`(186)、`remove_child`(203)、`sibling_props_change`(217)——**实际存在**，与 D2 §9 声称完全一致。✓
- §8 Snapshot `instances: BTreeMap<u64, PropValue>`（snapshot.rs:20）+ id.0（35-40）——与 D2 §8 一致（D10 前就如此）。✓

### D3 渲染（✅ 一致）
- §2 `DrawCmd{FillRect, DrawText}`、`SceneGraph cmds` + 方法（new/from_cmds/from_view/cmds/push/red_filled_rect）——与 scene_graph.rs 一致。✓
- §3 `from_view`（容器尺寸/DEFAULT_CONTAINER、LayoutEngine::compute_children、父偏移累加、Color→FillRect/Str→DrawText **亮白 Color::rgb(255,255,255)**（scene_graph.rs:115））——与代码一致。✓
- §4 LayoutEngine（compute/compute_children/LayoutResult/LayoutNode/LayoutStyle）——与 layout 一致。✓
- §5 单一 vello（RenderBackend{Vello}/render_to_view/render_offscreen/render_surface/create_surface/GpuSurface）——与 vello.rs 一致。✓
- §6 字形（GlyphKey/GlyphCacheEntry 占位/GlyphAtlas 占位/TextShaper shape_line 真实）——与 glyph.rs/text.rs 一致。✓
- §7 当前实现状态（GridAtlas/path_tessellation 占位/唯 vello/skia 删）——写实准确。✓

### D4 样式（✅ 一致，P1 未实现标注属实）
- §2 StyleSheet{StyleRule}/parse_rgss 占位返回默认空——与 style/mod.rs 一致（parse_rgss 不解析属实）。✓
- §3 未实现（P1：真实解析/主题/热重载）——写实准确。✓
- §1 热重载 P1，无 cssparser/notify——属实。✓

### D5 事件（❌ 不一致，1 处）
- **不符点**：§2 `input.rs` 声称 `pub enum InputModality { Pointer, Keyboard, Touch }`（**3 变体含 Touch**）；**代码实际 `pub enum InputModality { Pointer, Keyboard }`**（input.rs:6-11，**只有 2 变体，无 Touch**）。
- **相符点**：`InputEvent::CursorMoved/Pressed/Released/Text`（input.rs:18-26）与 D5 §2 一致 ✓；`FocusManager{focused:Option<WidgetId>}`+`set_modality` 空 body+`set_focus`（focus.rs:8-25）一致 ✓；§2 window.rs/event_loop.rs 描述一致 ✓；§3 App::run mapper 链路一致 ✓；§4 hit-test P1（未实现）属实 ✓；§5 依赖隔离（core 不引用 winit、platform default=["winit"]）属实 ✓。
- **判定**：仅 `InputModality` 变体列表与代码不符（文档多算了 Touch）。**P2 描述性偏差**（不影响编译/功能，D5 §2 应删 Touch）。

### D6 无障碍（✅ 一致）
- §2 AccessibilityRole{Unknown/Button/Text/Container}（a11y.rs:7-19）——与 D6 §2 一致 ✓；AccessibilityAction{Activate/Focus/Blur}、AccessibilityState{focusable,focused}、AccessibilityNode{role,label,state}+new/none——与 a11y.rs 一致 ✓。
- §3 AccessibilityTree{root}+TreeNode{widget_id,node,children}（a11y_tree.rs:8-17）——与 D6 §3 一致 ✓。
- §5 当前实现状态（纯 Rust 类型、AccessKit 桥已删、树未接入 GUI）——写实准确。✓

### D7（有意缺失，✅ 判断通过）
- docs/ 下无 D7-*.md。**合理**：D7 主题为 devtools/热重载/开发反馈，已被 greenfield 决策整体删除（无 devtools crate、无热重载），不建空文档符合"一个能力一条路径、凡不被需要不做"原则。**判断：有意缺失合理，无需补建。**

### D8 任务分解（✅ 一致）
- §1 里程碑 D3-D10 状态全部 ✅通过——与我 D3-D10 审查结论一致（各阶段 P0 清零放行）。✓
- §2 各阶段要点（D3 scaffold/D4 核心循环+状态+布局/D5 vello/D6 from_view/D7 窗口/D8 收敛/D9 字形+按需重绘/D10 组件）——与开发实况一致。✓
- §3 遗留 P2（mapper 无 hit-test/跨平台 display_handle/展开态文本截断/offscreen 手工 rect）——与 D10 审查确认的 P2 清单一致。✓
- §4 验收证据、45 测试——实测属实（见 §三）。✓

### D9 测试策略（✅ 一致，45 数字实测属实）
- §2 测试矩阵（core 单元 12 / core_loop 3 / d10_components 4 / layout_engine 2 / doc-test 1 ignored）——**实测 `cargo test -p rgui-core` = 12+3+4+2=21 通过**，与 D9 §2 "core 默认 21" **完全一致**。✓
- §5 全量"45 全绿"——**实测 `cargo test --workspace --all-features` = 逐项累加 45 通过（core 12 + core_loop 3 + d10 4 + layout 2 + render lib 1 + d6_e2e 5 + d6_from_view 8 + e2e_from_view 3 + glyph_offscreen 4 + nested_layout 1 + offscreen 2 = 45）**，与 D9 §5 **完全一致**。✓
- §4 截图验证法（d10_final_collapsed/expanded、--expanded 参数）——与 window_demo.rs:34-36 一致。✓

### D10 组件规范（✅ 一致）
- §3.1 Accordion（Toggle/AccordionState{title,subtitle,expanded}/340 高度展开130收起44/Colom rgb(90,130,220)/update 翻转）——与 components.rs 一致。✓
- §3.2 WaBadge（WaBadgeMsg 空枚举/label+count/160×40/rgb(120,160,210)）——与 components.rs 一致。✓
- §4 window_demo（App::run(config,Accordion,state,mapper)、--expanded、config.with_size(480,320)）——与 window_demo.rs 一致。✓
- §5 当前实现状态（Tier1 组件+示例、P1 多组件命中/Callback 占位/derive 宏透传）——写实准确。✓

---

## 二、D7 缺失判断

**✅ 有意缺失合理**。D7 对应 devtools/热重载/开发反馈，已被 greenfield 决策删除（无 devtools crate、无双热重载、无 .rgui 脚本路径）。建空文档无内容可写，且违背"凡不被需要的能力一律不做"。**不补建正确。**

---

## 三、数字实测核对结果（D9 声称）

| 声称 | 实测 | 结论 |
|---|---|---|
| core 默认 feature 21 测试通过（12+3+4+2） | `cargo test -p rgui-core` = 12+3+4+2 = **21 passed**（+1 ignored doc-test） | ✅ 属实 |
| 全量 --all-features 45 全绿 | `cargo test --workspace --all-features` = **45 passed**（12+3+4+2+1+5+8+3+4+1+2），0 失败 | ✅ 属实 |

**D9 §2/§5、D8 §4、D10 §45 声称的测试数字全部实测核实无误。** 非只信文档。

---

## 四、MERGE GATE 判定

**全框架文档与代码一致 → 放行（PASS）。**

- **P0 / P1：均无。**
- **唯一不符点**：D5 §2 `InputModality` 声称 3 变体（含 Touch），代码实际 2 变体（Pointer/Keyboard，无 Touch）。**P2 描述性偏差**——建议 doc 修正 D5 §2（删 Touch），即可全框架 100% 一致。
- D7 有意缺失合理确认。
- 测试数字（21/45）实测属实。

**建议**：随下一轮补 D 系列收尾时，doc 顺手修 D5 §2（InputModality 删 Touch 变体），其余全部通过。

---

*核对方：devco-reviewer｜只读核对，未改任何文档/代码。D1-D6/D8-D10 七份中 6 份一致、D5 有 1 处 P2 描述偏差（InputModality 多算 Touch）；D7 有意缺失合理；测试数字（core 21/全量 45）经 cargo test 实测属实。全框架文档与代码一致，放行。*
