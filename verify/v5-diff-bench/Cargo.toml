[package]
name = "verify-v5-diff-bench"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
name = "diff_bench"
path = "src/lib.rs"

[[bench]]
name = "diff"
harness = false

[dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
