[package]
name = "{{project-name}}"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"

[dependencies]
rgui = "0.1"

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
